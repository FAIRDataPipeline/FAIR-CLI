read:
- Datetime - ${{ CLI.DATETIME }}
- Repo/${{ CLI.SOURCE_CONFIG }}
- user: ${{CLI.USER}}
write:
- RandomText@?${{ CLI.GIT_BRANCH }}
- origin21231${{ CLI.GIT_REMOTE_ORIGIN }}
run_metadata:
  author_id: ${{CLI.USER_ID}}
  conf_dir: ${{ CLI.CONFIG_DIR }}
